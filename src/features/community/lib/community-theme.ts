// Digital Estate "cream paper" surface — the Community background, shared by the
// WebView, the native containers behind it, and the persistent panel chrome so
// the user never sees a white flash before Circle paints.
export const COMMUNITY_BACKGROUND_COLOR = '#fcf9f2';
